# 智能启动脚本使用指南

## 🚀 功能特性

新的启动脚本 (`npm run dev`) 具有以下智能功能：

### ✨ 主要功能

1. **自动端口检测和切换**
   - 优先使用端口 3000
   - 如果被占用，自动尝试 3001, 3002, 3003, 3004, 3005
   - 如果都被占用，随机选择 8000-9000 之间的端口

2. **自动清理缓存**
   - 启动前自动清理 `.next` 和 `.next-dev` 目录
   - 避免缓存导致的各种问题

3. **智能进程管理**
   - 检测到端口占用时，尝试自动终止占用进程（Windows）
   - 优雅的错误处理和重试机制

4. **友好的输出界面**
   - 彩色日志输出
   - 清晰的状态提示
   - 启动成功后显示访问地址

---

## 📖 使用方法

### 基本使用

```bash
# 智能启动（推荐）
npm run dev

# 直接启动（不进行任何检查）
npm run dev:direct
```

### 启动流程

```
1. 清理缓存目录
   ↓
2. 检测端口占用情况
   ↓
3. 查找可用端口
   ↓
4. 尝试释放首选端口（如果被占用）
   ↓
5. 启动开发服务器
   ↓
6. 显示访问地址
```

---

## 🎨 输出示例

### 正常启动

```
==================================================
  Excel Review App - 智能启动脚本  
==================================================

ℹ 正在清理缓存目录...
✓ 已清理: .next
✓ 已清理: .next-dev
ℹ 正在查找可用端口...
✓ 找到可用端口: 3000
ℹ 正在启动开发服务器 (端口 3000)...
✓ 开发服务器已启动！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  本地访问: http://localhost:3000
  网络访问: http://<your-ip>:3000
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

按 Ctrl+C 停止服务器
```

### 端口被占用时

```
==================================================
  Excel Review App - 智能启动脚本  
==================================================

ℹ 正在清理缓存目录...
✓ 已清理: .next
ℹ 正在查找可用端口...
⚠ 端口 3000 已被占用
✓ 找到可用端口: 3001
ℹ 正在启动开发服务器 (端口 3001)...
✓ 开发服务器已启动！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  本地访问: http://localhost:3001
  网络访问: http://<your-ip>:3001
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

按 Ctrl+C 停止服务器
```

### 自动释放端口

```
==================================================
  Excel Review App - 智能启动脚本  
==================================================

ℹ 正在清理缓存目录...
✓ 已清理: .next
ℹ 正在查找可用端口...
⚠ 端口 3000 已被占用
⚠ 首选端口 3000 被占用
ℹ 尝试释放端口 3000...
✓ 成功释放端口 3000
✓ 找到可用端口: 3000
ℹ 正在启动开发服务器 (端口 3000)...
✓ 开发服务器已启动！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  本地访问: http://localhost:3000
  网络访问: http://<your-ip>:3000
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

按 Ctrl+C 停止服务器
```

---

## ⚙️ 配置选项

启动脚本的配置在 `scripts/dev-start.js` 文件中：

```javascript
const CONFIG = {
  preferredPorts: [3000, 3001, 3002, 3003, 3004, 3005], // 优先端口列表
  cacheDirectories: ['.next', '.next-dev'],              // 要清理的缓存目录
  maxRetries: 3,                                          // 最大重试次数
  retryDelay: 2000,                                       // 重试延迟（毫秒）
};
```

### 自定义端口列表

如果你想使用不同的端口，编辑 `preferredPorts` 数组：

```javascript
preferredPorts: [8080, 8081, 8082, 8083],
```

### 禁用缓存清理

如果不想自动清理缓存，可以清空 `cacheDirectories` 数组：

```javascript
cacheDirectories: [],
```

---

## 🐛 故障排除

### 问题 1: 端口仍然被占用

**症状**: 脚本无法找到可用端口

**解决方案**:
1. 手动终止占用端口的进程：
   ```bash
   # Windows
   netstat -ano | findstr :3000
   taskkill /F /PID <PID>
   
   # Linux/Mac
   lsof -i :3000
   kill -9 <PID>
   ```

2. 或者重启电脑

### 问题 2: 缓存清理失败

**症状**: 看到 "无法清理" 的警告

**解决方案**:
1. 手动删除缓存目录：
   ```bash
   # Windows
   rmdir /s /q .next
   rmdir /s /q .next-dev
   
   # Linux/Mac
   rm -rf .next .next-dev
   ```

2. 确保没有其他进程占用这些目录
3. 以管理员权限运行

### 问题 3: 脚本启动失败

**症状**: 脚本报错退出

**解决方案**:
1. 使用直接启动模式：
   ```bash
   npm run dev:direct
   ```

2. 检查 Node.js 版本（需要 v14+）：
   ```bash
   node --version
   ```

3. 重新安装依赖：
   ```bash
   npm install
   ```

### 问题 4: 权限错误

**症状**: EPERM 或 EACCES 错误

**解决方案**:
1. **Windows**: 以管理员身份运行终端
2. **Linux/Mac**: 使用 sudo（不推荐）或修复目录权限：
   ```bash
   sudo chown -R $USER:$USER .
   ```

---

## 🔧 高级用法

### 指定端口启动

虽然脚本会自动选择端口，但你也可以手动指定：

```bash
# 方式 1: 使用环境变量
PORT=3005 npm run dev

# 方式 2: 直接使用 Next.js
npx next dev -p 3005
```

### 调试模式

如果需要查看更详细的输出：

```bash
# 启用 Next.js 的详细日志
DEBUG=* npm run dev
```

### 跳过缓存清理

如果你想保留缓存：

```bash
# 使用直接启动
npm run dev:direct
```

---

## 📊 性能对比

| 启动方式 | 缓存清理 | 端口检测 | 自动切换 | 错误处理 |
|---------|---------|---------|---------|---------|
| `npm run dev` (智能) | ✅ | ✅ | ✅ | ✅ |
| `npm run dev:direct` | ❌ | ❌ | ⚠️ | ⚠️ |
| `next dev` | ❌ | ❌ | ⚠️ | ❌ |

**推荐**: 日常开发使用 `npm run dev`

---

## 🎯 最佳实践

### 1. 首次启动项目

```bash
# 1. 安装依赖
npm install

# 2. 构建项目
npm run build

# 3. 启动开发服务器
npm run dev
```

### 2. 遇到问题时

```bash
# 1. 停止所有 Node 进程
# Windows: 任务管理器结束所有 Node.js 进程
# Linux/Mac: killall node

# 2. 清理缓存
npm run dev  # 会自动清理

# 3. 如果还有问题，重新构建
npm run build
npm run dev
```

### 3. 多项目同时开发

脚本会自动找到可用端口，所以你可以同时运行多个项目：

```bash
# 项目 A (端口 3000)
cd project-a
npm run dev

# 项目 B (端口 3001)
cd project-b
npm run dev

# 项目 C (端口 3002)
cd project-c
npm run dev
```

---

## 🔒 安全注意事项

### 自动终止进程功能

脚本在 Windows 上会尝试自动终止占用首选端口的进程。这个功能：

- ✅ 方便快速启动
- ⚠️ 可能会终止其他重要进程
- 💡 建议在开发环境使用
- 🚫 不建议在生产环境使用

如果你不想使用这个功能，可以在 `scripts/dev-start.js` 中注释掉相关代码：

```javascript
// 注释掉这部分
// if (port !== CONFIG.preferredPorts[0]) {
//   const preferredPort = CONFIG.preferredPorts[0];
//   logWarning(`首选端口 ${preferredPort} 被占用`);
//   
//   if (process.platform === 'win32') {
//     logInfo(`尝试释放端口 ${preferredPort}...`);
//     await killProcessOnPort(preferredPort);
//     ...
//   }
// }
```

---

## 📚 相关文档

- **Next.js 文档**: https://nextjs.org/docs
- **项目 README**: `README.md`
- **集成文档**: `INTEGRATION_COMPLETE.md`
- **最终总结**: `FINAL_SUMMARY.md`

---

## 🆘 获取帮助

如果遇到问题：

1. 查看本文档的"故障排除"部分
2. 检查控制台输出的错误信息
3. 尝试使用 `npm run dev:direct` 直接启动
4. 查看 Next.js 官方文档

---

**最后更新**: 2025-01-13  
**版本**: 1.0.0
