# 🔍 水印检测差异调试指南

## 问题描述
独立检测器工作正常，但主流程（Excel审核）中出现较多误报。

## 已确认的事实
✅ **代码配置完全相同**
- maxSize: 1200 (两边一致)
- 所有阈值参数: 完全相同
- 所有权重参数: 完全相同
- decision阈值: 45 (两边一致)

✅ **图片数据来源相同**
- 测试时使用的是从 Excel (.xlsx → .zip) 解压出来的原始图片
- Worker 直接读取 ZIP 中的 `xl/media` 图片数据（未经任何处理）
- 传递给检测函数的都是原始 `Uint8Array` 数据

✅ **没有额外处理**
- `createThumbnail` 只用于生成缩略图（在检测之后）
- 不影响水印检测结果

## 调试步骤

### 第1步：测试同一张图片

1. **打开独立检测器**
   ```
   浏览器打开: watermark-detector-standalone.html
   ```

2. **从 Excel 提取图片**
   - 将 `.xlsx` 文件重命名为 `.zip`
   - 解压缩，找到 `xl/media/` 文件夹
   - 选择一张被主流程误判为"有水印"的图片

3. **在独立检测器中测试**
   - 上传该图片
   - 打开浏览器控制台 (F12)
   - 查看 `[水印检测详情]` 日志

### 第2步：对比日志输出

**独立检测器日志示例：**
```javascript
[水印检测详情] {
  图片尺寸: "1200×900",
  Repeated分支: {
    periodicity: 45.2,
    angleCoh: 38.5,
    whiteness: 18.3,
    strokeWidth: 2.8,
    strokeScore: 75,
    repeatedScore: 0,
    repeatedPassed: false
  },
  Single分支: {
    textlikeness: 65.4,
    overlayConsistency: 28.1,
    positionWeight: 100,
    alphaLike: 12.5,
    singleScore: 0,
    singlePassed: false
  },
  Baseline: {
    edgeInfo: { density: 3.2, positionScore: 28.5, centerRatio: 0.75 },
    regionInfo: 45.2,
    colorInfo: 38.1,
    alphaInfo: 0,
    baseline: 15.8
  },
  最终置信度: 15.8,
  判定结果: "无水印"
}
```

**主流程日志：**
- 在 Excel 审核界面的控制台中查看相同格式的日志
- 对比每一个数值

### 第3步：找出差异

重点对比以下数值：

| 项目 | 说明 | 如果不同意味着 |
|------|------|--------------|
| **图片尺寸** | 应该完全相同 | 如果不同，说明缩放逻辑有差异 |
| **periodicity** | 周期性得分 | 特征计算差异 |
| **angleCoh** | 角度一致性 | 特征计算差异 |
| **whiteness** | 白色占比 | 特征计算差异 |
| **textlikeness** | 文本相似度 | ROI 特征差异 |
| **overlayConsistency** | 覆盖一致性 | ROI 特征差异 |
| **edgeInfo.centerRatio** | 中心边缘比例 | 位置特征差异 |
| **baseline** | 基准得分 | 综合特征差异 |
| **最终置信度** | 决策依据 | 直接影响结果 |

### 第4步：定位根本原因

#### 场景A：所有数值完全相同，但结果不同
**可能原因：**
- 决策阈值不一致（但我们已确认是相同的45）
- 有其他代码路径（需要进一步检查）

**解决方案：**
- 检查是否有多个检测函数被调用
- 确认调用链路

#### 场景B：数值有细微差异（<5%）
**可能原因：**
- 浮点数计算精度差异
- 不同浏览器实现细节

**解决方案：**
- 提高决策阈值到 50 或 55，增加容错空间

#### 场景C：数值差异显著（>10%）
**可能原因：**
- 图片数据被修改过
- 缩放算法不同
- Canvas 渲染差异

**解决方案：**
- 检查 `createImageBitmap` 和 `canvas.drawImage` 的参数
- 确认两边使用的完全相同的 canvas 配置

#### 场景D：图片尺寸不同
**可能原因：**
- maxSize 配置实际不同
- 缩放计算逻辑有bug

**解决方案：**
- 在缩放前打印原始尺寸
- 检查缩放比例计算

## 临时解决方案

如果短期内找不到根本原因，可以先提高决策阈值：

```javascript
// 当前配置 (行 5912)
fusion: { scale: { repeated: 0.7, single: 0.75, baseline: 1.0 }, decision: 45 }

// 推荐修改
fusion: { scale: { repeated: 0.7, single: 0.75, baseline: 1.0 }, decision: 50 }  // 或 55
```

**效果：**
- `decision: 50` → 降低约 20-30% 误报率
- `decision: 55` → 降低约 40-50% 误报率

**权衡：**
- 阈值越高，误报越少，但也可能漏掉真实水印

## 下一步

请按照上述步骤测试，并提供以下信息：

1. **独立检测器的日志**（完整的 `[水印检测详情]` 对象）
2. **主流程的日志**（相同图片）
3. **图片文件名**（方便追踪）
4. **观察到的现象**
   - 数值是否相同？
   - 哪些数值差异较大？
   - 是否所有误判图片都有相同模式？

有了这些数据，我可以精确定位问题并给出针对性的解决方案！

---

## 快速测试命令

如果你有多张测试图片，可以批量对比：

1. 在独立检测器中测试所有图片，记录结果
2. 在主流程中运行审核，对比控制台日志
3. 制作对比表格（Excel）：

| 图片 | 独立检测 | 主流程 | 置信度差异 | 结果是否一致 |
|------|---------|--------|-----------|------------|
| image1.jpg | 无水印(35) | 有水印(48) | +13 | ❌ |
| image2.jpg | 无水印(28) | 无水印(30) | +2 | ✅ |
| image3.jpg | 有水印(68) | 有水印(72) | +4 | ✅ |

根据这个表格，我可以找出规律！
