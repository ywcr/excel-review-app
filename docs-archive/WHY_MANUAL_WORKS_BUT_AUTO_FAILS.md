# 为什么手动创建成功，自动化失败？

## 问题

用户问："那为何手动创建可以？"

## 答案

**手动创建和自动化创建使用的是不同的问卷，答案长度不同！**

## 详细对比

### 成功的手动创建（之前的问卷）

**问卷类型：** 可能是"致力庆西黄丸消费者问卷"的**旧版本**或**不同版本**

**答案内容（短）：**

```
answer0=35~59
answer1=服务周到
answer2=提供更完善的药学服务
answer3=店员形象
answer4=一般
answer5=很专业
answer6=从不
answer7=从不
answer8=否
answer9=价格
```

**答案特点：**

- ✅ 简短的中文词汇
- ✅ 2-8 个字符
- ✅ 没有特殊符号（除了波浪号 `~`）

**encryptedText（前 255 字符）：**

```
answer0=35~59&answer1=服务周到&answer2=提供更完善的药学服务&answer3=店员形象&answer4=一般&answer5=很专业&answer6=从不&answer7=从不&answer8=否&answer9=价格&answers=35~59#服务周到#提供更完善的药学服务#店员形象#一般#很专业#从不#从不#否#价格#channelAddress=&corpId=1749721838789101&date=2025-09-06&dcdxName=吴承&fieldName
```

**关键参数位置：**

- ✅ `channelAddress` 在第 167 个字符
- ✅ `corpId` 在第 184 个字符
- ✅ `date` 在第 211 个字符
- ✅ `dcdxName` 在第 233 个字符
- ✅ 所有关键参数都在 255 字符以内！

**结果：** ✅ 验签成功

---

### 失败的自动化创建（当前问卷）

**问卷类型：** "致力庆西黄丸消费者问卷"的**新版本**

**答案内容（长）：**

```
answer0=中医辨证热毒壅结证
answer1=提到过但未强调
answer2=放疗 / 化疗
answer3=其他
answer4=未解释，直接推荐
answer5=未留意过储存相关说明
answer6=看过说明书但未重视
answer7=服药后胃部轻微不适
answer8=习惯购买该品牌中成药
answer9=略高于预期
```

**答案特点：**

- ❌ 较长的中文短语
- ❌ 8-12 个字符
- ❌ 包含特殊符号（`/`, `，`）

**encryptedText（前 255 字符）：**

```
answer0=中医辨证热毒壅结证&answer1=提到过但未强调&answer2=放疗 / 化疗&answer3=其他&answer4=未解释，直接推荐&answer5=未留意过储存相关说明&answer6=看过说明书但未重视&answer7=服药后胃部轻微不适&answer8=习惯购买该品牌中成药&answer9=略高于预期&answers=中医辨证热毒壅结证#提到过但未强调#放疗 / 化疗#
```

**关键参数位置：**

- ❌ `channelAddress` 在第 **256+** 个字符（被截断）
- ❌ `corpId` 在第 **270+** 个字符（被截断）
- ❌ `date` 在第 **300+** 个字符（被截断）
- ❌ `dcdxName` 在第 **320+** 个字符（被截断）
- ❌ 所有关键参数都在 255 字符之后！

**结果：** ❌ 验签失败

---

## 根本原因

### 1. 问卷版本不同

手动创建时使用的问卷和当前自动化使用的问卷**不是同一个版本**！

**证据：**

- 旧版问卷的问题：

  ```
  1. 您的年龄？ → 答案：35~59
  2. 您选择同仁堂补中益气丸的核心原因是？ → 答案：服务周到
  3. 您认为您的药品知识符合以下哪种情况？ → 答案：提供更完善的药学服务
  ```

- 新版问卷的问题（当前）：
  ```
  1. 您使用西黄丸的主要目的是？ → 答案：中医辨证热毒壅结证
  2. 医生 / 药师是否告知您西黄丸含麝香，孕妇禁用？ → 答案：提到过但未强调
  3. 服用西黄丸时，您是否同时接受以下治疗？ → 答案：放疗 / 化疗
  ```

**这是两个完全不同的问卷！**

### 2. 答案长度差异

| 对比项       | 旧版问卷（成功） | 新版问卷（失败） |
| ------------ | ---------------- | ---------------- |
| 平均答案长度 | 3-5 字符         | 8-12 字符        |
| 特殊符号     | 少（`~`）        | 多（`/`, `，`）  |
| 总答案长度   | ~150 字符        | ~250+ 字符       |
| 关键参数位置 | 255 字符内       | 255 字符外       |
| 验签结果     | ✅ 成功          | ❌ 失败          |

### 3. 参数排序问题

`formatParams` 按字母排序，导致：

- `answer*` (字母 'a') → 排在最前面
- `channelAddress`, `corpId`, `date` → 排在后面

当答案内容长时，前 255 字符只包含答案，关键参数被截断。

---

## 为什么手动创建可以？

### 情况 1：使用的是旧版/不同版问卷

如果你手动创建时使用的是答案较短的问卷版本，那么：

- 前 255 字符包含了所有关键参数
- 验签成功
- 创建成功

### 情况 2：手动创建时没有截取 255 字符

如果你手动创建时直接在浏览器控制台执行，可能：

- 没有经过 dcwj.js 的 255 字符截取逻辑
- 或者浏览器的表单提交自动处理了这个问题
- 或者后端在某些情况下会容错处理

### 情况 3：手动创建使用的是 DOM 模式

如果你手动创建时是通过页面表单提交（DOM 模式），那么：

- 不需要签名验证
- 或者使用不同的签名逻辑
- 直接提交表单数据

---

## 解决方案

### 方案 1：使用 DOM 模式（推荐）

对于当前这个包含长答案的问卷，使用 DOM 模式：

```javascript
start(); // 手动执行单个任务
automatic(); // 自动执行所有任务
```

**优点：**

- ✅ 不受 255 字符限制
- ✅ 不需要签名验证
- ✅ 支持任意长度的答案
- ✅ 更稳定可靠

### 方案 2：简化答案选项（不现实）

修改问卷设计，使用更短的答案选项。

**缺点：**

- ❌ 需要修改问卷内容
- ❌ 可能影响问卷质量
- ❌ 不由我们控制

### 方案 3：修改后端签名逻辑（需要协调）

与后端协调，修改签名逻辑：

- 移除 255 字符限制
- 或者只对关键参数签名
- 或者使用不同的签名策略

**缺点：**

- ❌ 需要修改后端代码
- ❌ 需要协调多方
- ❌ 可能影响安全性

---

## 总结

**手动创建成功的原因：**

1. 使用的是**不同版本的问卷**（答案较短）
2. 或者使用的是 **DOM 模式**（不需要签名）
3. 或者后端在某些情况下**容错处理**

**自动化失败的原因：**

1. 当前问卷的**答案内容过长**
2. **255 字符限制**导致关键参数被截断
3. **验签失败**

**最佳解决方案：**

- 对于长答案问卷：使用 **DOM 模式** ✅
- 对于短答案问卷：使用 **API 模式** ✅

---

## 验证方法

### 验证手动创建使用的问卷

1. 查看手动创建时的问卷页面
2. 记录问题和答案选项
3. 对比当前自动化使用的问卷
4. 确认是否是同一个问卷

### 验证答案长度

```javascript
// 计算答案总长度
const answers = [
  "中医辨证热毒壅结证",
  "提到过但未强调",
  "放疗 / 化疗",
  "其他",
  "未解释，直接推荐",
  "未留意过储存相关说明",
  "看过说明书但未重视",
  "服药后胃部轻微不适",
  "习惯购买该品牌中成药",
  "略高于预期",
];

const totalLength = answers.join("#").length;
console.log("答案总长度:", totalLength);
// 输出：答案总长度: 约 150+ 字符

// 加上 answer0=, answer1=, &, answers= 等
// 总长度约 250-300 字符
```

---

## 修复日期

2025-10-05
