# ⚡ 水印检测 - 快速启用指南

## 🎯 3步查看水印检测日志

### 第1步：启用功能开关 ✅

上传Excel文件后，**必须勾选这两个选项**：

```
验证选项
├─ ✅ 包含图片验证（清晰度检测和重复检测）
└─ └─ ✅ 启用水印检测 🔍  ← 这个必须勾选！
        └─ (实验性功能，可能增加检测时间)
```

⚠️ **重要**：默认情况下水印检测是**关闭**的！

---

### 第2步：打开浏览器Console 📊

按 **F12** 或 **Ctrl+Shift+I** 打开开发者工具

然后点击 **Console** 标签

---

### 第3步：开始审核并观察日志 🔍

点击 "**开始审核**" 按钮

你将看到类似这样的日志：

```
[水印检测] 开始高级检测流程...
[水印检测] 执行像素级分析...
[水印检测] 分析结果: { frequency: 12.5, gradient: 15.3, ... }
[水印检测] 完成！耗时: 245.67ms, 置信度: 52.8
```

---

## 🎨 界面截图提示

### 启用前（默认状态）

```
┌──────────────────────────────────────┐
│ 验证选项                              │
├──────────────────────────────────────┤
│ ☐ 包含图片验证（清晰度检测和重复检测）│  ← 未勾选
└──────────────────────────────────────┘
```

### 启用后（正确状态）

```
┌──────────────────────────────────────┐
│ 验证选项                              │
├──────────────────────────────────────┤
│ ☑ 包含图片验证（清晰度检测和重复检测）│  ← 已勾选
│   ☑ 启用水印检测 🔍                   │  ← 已勾选（关键！）
│     (实验性功能，可能增加检测时间)     │
└──────────────────────────────────────┘
```

---

## 📋 Console 过滤技巧

在 Console 顶部的过滤框输入：

```
水印
```

只显示水印相关日志 ✨

---

## 🐛 常见问题

### Q: 看不到日志？

**A:** 确保两个选项都勾选了！特别是 "**启用水印检测 🔍**"

### Q: 日志被清空了？

**A:** 在 Console 中右键 → 勾选 "**Preserve log**"

### Q: 检测太慢？

**A:** 这是正常的，水印检测需要深度像素分析
- 小图片：~100ms
- 大图片：~500ms

---

## 🎯 快速验证

### 步骤：
1. `npm run dev`
2. 打开 http://localhost:3001
3. 上传Excel文件
4. ✅ 勾选两个选项
5. F12 打开Console
6. 点击"开始审核"
7. 观察日志

### 成功标志：
- ✅ 看到 `[水印检测] 开始高级检测流程...`
- ✅ 看到详细分析结果
- ✅ 看到 `[水印检测] 完成！`

---

## 📊 日志示例

```javascript
// 每张图片都会输出这些日志：

[水印检测] 开始高级检测流程...
[水印检测] 执行像素级分析...

[水印检测] 分析结果: {
  frequency: 12.50,       // 频域分析
  gradient: 15.30,        // 梯度分析
  texture: 8.20,          // 纹理分析
  colorChannel: 6.70,     // 颜色通道
  region: 10.40,          // 区域分析
  alpha: 0.00             // 透明度
}

[水印检测] 完成！耗时: 245.67ms, 置信度: 52.8
// 置信度 >= 45 表示检测到水印 ✓
// 置信度 < 45 表示无水印 ✓
```

---

## 🎚️ 调整灵敏度（可选）

如果你想调整检测阈值：

**位置**：`public/validation-worker.js` 第 5122 行

```javascript
// 默认阈值 45%
const hasWatermark = confidence >= 45;

// 更灵敏（更容易检测到）
const hasWatermark = confidence >= 35;

// 更严格（减少误报）
const hasWatermark = confidence >= 55;
```

---

## ✅ 完成！

现在你可以：
- ✅ 看到详细的水印检测日志
- 📊 了解每个检测指标的得分
- 🔍 知道哪些图片有水印
- 🎚️ 调整检测参数（如果需要）

---

**更多详细信息**：参见 `WATERMARK_DETECTION_TEST_GUIDE.md`

**功能状态**：✅ 已集成，默认关闭  
**启用方式**：勾选UI中的开关  
**日志位置**：浏览器开发者工具 Console
