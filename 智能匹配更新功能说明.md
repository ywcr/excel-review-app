# 智能匹配更新功能说明

## 功能概述

新增了 `updateByOrderApi()` 函数，可以智能匹配工单列表和 data 数组，按照 data 的顺序精确更新对应的工单。

## 功能特点

### 1. 智能匹配
- 自动查询所有工单列表（或指定日期的工单）
- 按照 data 数组的顺序逐个匹配对应的 workOrderId
- 支持同名人员的多工单处理（按顺序使用）

### 2. 间隔控制
- 使用 `apiRequestInterval` 设置的间隔时间
- 默认 5000ms（5秒），可通过 `setApiInterval()` 调整
- 避免请求过快导致服务器限流

### 3. 进度追踪
- 实时显示更新进度
- 统计成功、失败、跳过的数量
- 支持从指定位置继续执行

## 使用方法

### 基础用法

```javascript
// 1. 更新所有工单（按 data 顺序匹配）
updateByOrderApi()

// 2. 更新指定日期的工单
updateByOrderApi("10.09")

// 3. 从第5个开始更新所有工单
updateByOrderApi(null, 5)

// 4. 从"张三"开始更新指定日期工单
updateByOrderApi("10.09", "张三")

// 5. 从第10个开始更新所有工单
updateByOrderApi(null, 10)
```

### 与间隔设置配合使用

```javascript
// 设置更新间隔为3秒
setApiInterval(3000)

// 然后执行更新
updateByOrderApi("10.09")
```

## 参数说明

### updateByOrderApi(mmdd, startFrom)

| 参数 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| mmdd | string \| null | 否 | 日期（MM.DD格式），null表示所有日期 | "10.09", null |
| startFrom | number \| string \| null | 否 | 起始位置（数字从1开始）或姓名 | 5, "张三", null |

## 执行流程

```
1. 查询工单列表
   └─ 获取 projectId
   └─ 调用 fetchWorkOrdersApi() 获取工单
   └─ 建立姓名 → workOrderId 映射表

2. 按 data 顺序匹配
   └─ 遍历 data 数组（从 startFrom 开始）
   └─ 根据姓名查找对应的 workOrderId
   └─ 处理同名情况（使用未使用的工单）

3. 逐个更新
   └─ 调用 updateTaskApi() 更新工单
   └─ 使用设置的间隔时间
   └─ 实时显示进度和结果
```

## 输出示例

```
🔄 开始智能匹配更新...
📋 查询工单列表 (日期: 10.09)...
✅ 查询到 28 个工单
📊 建立映射表：28 个不同姓名
📍 从第 5 个开始更新

🔄 [1/24] 更新: 李四 (女) - 工单ID: 1756789123456789
✅ [1/24] 更新成功: 李四
⏳ 等待 5000ms...

🔄 [2/24] 更新: 王五 (男) - 工单ID: 1756789123456790
✅ [2/24] 更新成功: 王五
⏳ 等待 5000ms...

...

📊 智能匹配更新完成：
   ✅ 成功: 22
   ❌ 失败: 1
   ⏭️  跳过: 1
   📝 总计: 24
```

## 对比其他更新方法

### updateByOrderApi() vs updateByDateApi()

| 功能 | updateByOrderApi | updateByDateApi |
|------|------------------|-----------------|
| **匹配方式** | 按 data 数组顺序精确匹配 | 查询工单后按工单顺序更新 |
| **适用场景** | 需要精确控制更新顺序 | 批量更新整个日期的工单 |
| **同名处理** | 按 data 顺序使用同名工单 | 使用工单列表中的顺序 |
| **间隔控制** | 使用 apiRequestInterval | 固定 300ms |
| **起始位置** | 支持从指定位置继续 | 不支持 |

## 典型使用场景

### 场景1：补充遗漏的工单
```javascript
// 验证后发现第10-15个工单需要重新更新
updateByOrderApi(null, 10)  // 从第10个开始更新
```

### 场景2：分批更新避免超时
```javascript
// 第一批：更新前20个
updateByOrderApi("10.09", 1)

// 等待完成后，第二批：更新第21个开始
updateByOrderApi("10.09", 21)
```

### 场景3：单独更新某人之后的数据
```javascript
// 从"张三"开始往后更新
updateByOrderApi("10.09", "张三")
```

## 注意事项

### 1. 数据匹配
- 匹配依据是 **姓名** 字段
- 如果 data 中的姓名在工单列表中找不到，会跳过并提示
- 同名工单会按 data 顺序依次使用

### 2. 工单状态
- 不检查工单当前状态，直接更新
- 已撤销的工单可能无法更新，会显示失败

### 3. 性能控制
- 使用 `setApiInterval()` 设置合理的间隔
- 建议 3-5 秒间隔，避免服务器限流
- 大批量更新时可以分批执行

### 4. 错误处理
- 单个工单更新失败不会中断整体流程
- 会记录失败数量并在最后统计显示
- 建议更新完成后再次验证数据

## 与验证功能配合使用

```javascript
// 1. 先验证数据，找出缺失和需要更新的记录
validateData()

// 2. 查看详细的缺失信息
showMissing()

// 3. 使用智能匹配更新功能补充
updateByOrderApi("10.09")

// 4. 更新完成后再次验证
validateData()
```

## 问题排查

### 问题1：提示"未找到对应工单"
**原因**：data 中的姓名在工单列表中不存在
**解决**：
1. 检查姓名是否正确
2. 确认日期筛选是否正确
3. 先执行 `validateData()` 查看哪些记录缺失

### 问题2：同名工单被跳过
**原因**：同名工单数量少于 data 中该姓名出现的次数
**解决**：
1. 检查是否有重复数据
2. 确认工单列表是否完整
3. 可以先创建缺失的工单，再更新

### 问题3：更新速度太慢
**原因**：间隔时间设置过大
**解决**：
```javascript
// 调整为较短的间隔（但不要太短，避免限流）
setApiInterval(2000)  // 2秒间隔
updateByOrderApi("10.09")
```

## 总结

`updateByOrderApi()` 是一个强大的智能匹配更新工具，特别适合：
- ✅ 精确控制更新顺序
- ✅ 补充遗漏的工单数据
- ✅ 分批更新大量数据
- ✅ 从特定位置继续执行

配合验证功能和间隔设置，可以高效、安全地完成批量更新任务。
