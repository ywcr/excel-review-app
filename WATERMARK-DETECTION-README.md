# 水印检测系统 - 医院/药店场景

## 🎯 快速开始

### 运行检测
```bash
# 批量检测所有图片
node test-watermark-images.js

# 输出结果保存在：
# temp/extracted-images/results-bplus.json
```

### 性能指标
- ✅ **召回率：100%** - 不会漏掉任何水印
- ⚠️ **准确率：64.5%** - 正确分类129/200张图片
- ❌ **假阳性：71张** - 需人工复核

---

## 📸 场景说明

### ⚠️ 重要：这不是检测Excel表格！

```
❌ 错误理解：检测Excel单元格、表格网格
✅ 正确理解：检测从Excel提取的图片中的水印
```

### 图片内容

所有图片均为**医院/药店场景照片**：
- 🏥 门头照：医院/药店外景
- 🏪 内部照：货架、展示柜、药品陈列
- 📦 细节照：药盒、证照、价格标签

### "网格"的真实含义

检测到的"Grid特征"**不是Excel网格线**，而是照片中的：
- 🛒 货架网格：金属货架的横竖框架
- 🏢 瓷砖网格：地面/墙面的瓷砖
- 📦 药盒排列：整齐陈列的商品
- 🪟 展示柜：玻璃柜框架

---

## 🔍 检测难点

### 真水印 vs 场景文字

| 类型 | 示例内容 | 位置 | 像素特征 |
|------|---------|------|---------|
| **真水印** | "© 2024 某某医院"<br>"某某药店拍摄" | 四角/边缘 | 高TL + 低OC + 网格背景 |
| **场景文字** | "规格：500mg"<br>"价格：¥128" | 货架角落 | 高TL + 低OC + 网格背景 |

**核心问题**：像素级特征几乎相同，只能靠**语义内容**区分！

---

## 📊 使用建议

### 适用场景

✅ **推荐用于**：
1. 初筛阶段：快速过滤明显无水印的图片（124/195正确）
2. 保证不漏检：100%召回率
3. 成本优化：避免对所有图片做昂贵的OCR

⚠️ **需注意**：
- 71个假阳性需要人工复核
- 无法通过像素特征完全区分水印和商品标签

### 生产流程

```
Step 1: 像素级检测 (test-watermark-images.js)
  ↓ 检测出76张疑似有水印
  
Step 2: 人工复核 (仅71个假阳性)
  ↓ 确认哪些是真水印
  
Step 3: 最终结果
  ✅ 5张真水印
  ❌ 71张误报 (实际是货架标签等)
```

---

## 🚀 突破瓶颈

### 当前瓶颈

**已到达像素级方法的理论极限**：
- 真水印和货架标签在位置、文字、透明度、网格背景等特征上几乎一致
- 唯一区别在于语义内容（版权 vs 商品信息）

### 进阶方案

#### 方案A：OCR + 语义分析（推荐）

```javascript
const text = extractText(roiImage);

// 关键词匹配
if (text.includes('©') || text.includes('版权')) {
  confidence += 50;  // 明确是水印
} else if (text.includes('规格') || text.includes('¥')) {
  confidence -= 30;  // 明确是商品信息
}
```

**预期效果**：精确率提升到80%+

#### 方案B：深度学习

训练CNN分类器，区分水印文字和商品标签。

**预期效果**：准确率95%+

---

## 📚 相关文件

### 主要文件

| 文件 | 用途 | 推荐度 |
|------|------|--------|
| `test-watermark-images.js` | 主检测器（**推荐使用**） | ⭐⭐⭐⭐⭐ |
| `WATERMARK-DETECTION-CONTEXT.md` | 详细场景说明文档 | ⭐⭐⭐⭐⭐ |
| `optimize-watermark-threshold.js` | 阈值优化工具 | ⭐⭐⭐ |
| `batch-test-watermarks.js` | 结果分析工具 | ⭐⭐⭐ |

### 不推荐使用

| 文件 | 原因 |
|------|------|
| `advanced-watermark-detection.js` | 浏览器环境专用,Node.js不适用 |

---

## 🔧 技术架构

```
输入：200张医院/药店照片
  ↓
[9维像素特征提取]
  文本相似度、透明度、位置、网格、集中度...
  ↓
[FFT频域分析] (辅助15%)
  周期性、高频异常、方向性
  ↓
[多分支融合]
  Repeated + Single + Baseline
  ↓
[10条硬拒绝规则]
  过滤货架标签、价格牌等
  ↓
[3条专门路径]
  cornerLogo、cornerText、edgeText
  ↓
输出：5张真水印 + 71张假阳性
```

---

## ❓ 常见问题

### Q1: 为什么有这么多假阳性？
**A**: 货架标签恰好在角落，文字特征和水印高度相似。像素级方法无法区分语义内容。

### Q2: 能否提高精确率？
**A**: 像素级方法已达瓶颈。需要引入OCR或深度学习来理解文字语义。

### Q3: "Grid特征"是什么？
**A**: 不是Excel表格网格，而是照片中的货架框架、瓷砖等真实场景元素。

### Q4: 为什么不能单独依赖FFT检测?
**A**: FFT对压缩图片过于敏感,容易产生误报。因此FFT仅作为15%的辅助特征,与像素级特征融合使用。

---

## 📝 总结

### 当前方案优势
✅ 100%召回率，不漏检  
✅ 无需外部依赖，纯视觉特征  
✅ 快速执行（<2秒/图）  
✅ 可解释性强  

### 已知限制
⚠️ 36%假阳性率  
⚠️ 需人工复核71张图片  
⚠️ 无法理解文字语义  

### 适用性评估
- ✅ **适用于初筛 + 人工复核流程**
- ❌ **不适用于完全自动化场景**

---

*最后更新：2025-01-17*  
*详细文档：WATERMARK-DETECTION-CONTEXT.md*
