# 水印检测场景说明

## 📋 项目背景

### 数据来源
- **输入**：Excel文件（.xlsx）
- **提取对象**：Excel文件中嵌入的图片（非Excel单元格内容）
- **图片数量**：200张图片
- **真实水印**：5张图片（image196.jpeg, image197.jpeg, image198.png, image199.jpg, image200.jpg）

### ⚠️ 重要澄清

**我们检测的是从Excel中提取出的独立图片文件，而不是Excel表格本身！**

```
❌ 错误理解：检测Excel表格单元格、网格线
✅ 正确理解：检测提取出的JPG/PNG图片中的水印
```

---

## 🏥 图片场景特征

### 图片类型

所有图片均为**医院/药店场景照片**：

#### 1. 门头照
- 医院/药店外景
- 招牌、门面
- 装修、环境

#### 2. 内部陈列照
- **药店货架**：整齐排列的药品
- **展示柜**：玻璃柜、陈列架
- **药品区域**：商品分类展示

#### 3. 细节照
- **药盒特写**：包装、标签
- **证照展示**：营业执照、药品许可证
- **价格标签**：商品标价牌

---

## 🎯 "网格特征" 的真实含义

### ❌ 不是：Excel表格网格
### ✅ 实际是：

| Grid来源 | 场景描述 | 视觉特征 |
|---------|---------|---------|
| **货架网格** | 药店金属货架的横竖框架 | 规则的水平/垂直线条结构 |
| **瓷砖网格** | 医院地面、墙面的瓷砖拼接 | 重复的矩形网格pattern |
| **药盒排列** | 整齐陈列的药品包装 | 规则矩形阵列 |
| **展示柜框架** | 玻璃柜、展架的金属/木质框架 | 横竖线条交叉 |
| **货架边框** | 商品陈列架的边框线 | 规则的矩形边界 |

**这些"网格"是照片拍摄的真实场景元素，不是数字化的Excel网格线！**

---

## 🔍 水印与场景元素的区分难点

### 真水印特征

```javascript
// 典型的真水印内容：
"© 2024 某某医院"
"某某药店拍摄"
"@摄影师名字"
"版权所有 禁止转载"
"拍摄时间：2024-XX-XX"
```

**特点**：
- 位置：通常在四个角落或边缘
- 样式：半透明、白色文字、阴影
- 内容：版权、署名、时间戳
- 目的：标识图片来源，防止盗用

### 假阳性（场景文字）

```javascript
// 容易被误判为水印的场景元素：
"规格：500mg × 24片"       // 药盒上的规格说明
"价格：¥128.00"            // 价格标签
"批号：20240315"           // 药品批号
"有效期至：2026-03"        // 保质期标签
"营业执照编号：..."        // 证照文字
"禁止拍照"                 // 店内提示牌
```

**特点**：
- 位置：恰好在货架边角、药盒角落
- 样式：印刷文字、标签贴纸
- 内容：商品信息、价格、规格
- 目的：商品标识、价格展示

---

## 📊 检测难点总结

### 为何难以区分？

| 维度 | 真水印 | 场景文字 | 像素级特征能否区分？ |
|------|--------|---------|-------------------|
| **位置** | 四角/边缘 | 四角/边缘（货架角落） | ❌ 相同 |
| **文字特征** | 高TL值 | 高TL值（印刷文字） | ❌ 相同 |
| **透明度** | 半透明/低OC | 纸质标签/低OC | ❌ 相同 |
| **集中度** | 1.0-2.0 | 0.5-2.0（标签大小） | ⚠️ 重叠 |
| **背景网格** | 有（货架） | 有（货架） | ❌ 相同 |
| **语义内容** | 版权、署名 | 规格、价格 | ✅ **可区分** |

**结论**：像素级特征在此场景下的瓶颈是**语义鸿沟**！

---

## ✅ 当前最优方案

### test-watermark-images.js

**性能指标**：
- ✅ 召回率：100% (5/5，不漏检任何水印)
- ⚠️ 准确率：64.5% (129/200正确分类)
- ❌ 假阳性：71张 (36%误报率)

**核心技术**：
1. **9维像素特征**：TL, OC, AL, PW, Grid, ROI Grid, Conc, EdgeEntropy, Whiteness
2. **FFT频域分析**：辅助验证（15%权重）
3. **10条硬拒绝规则**：精准过滤货架场景文字
4. **3条专门路径**：cornerLogo, cornerText, edgeText

**适用场景**：
- ✅ 初筛：过滤明显无水印图片（124/195正确）
- ✅ 保证不漏：100%召回率
- ⚠️ 需人工复核：71个假阳性需二次确认

---

## 🚀 突破瓶颈方向

### 方案A：OCR + 语义分析（推荐）

```javascript
// 提取ROI区域文字
const text = extractText(roiImage);

// 关键词匹配
const watermarkKeywords = ['版权', '©', 'Copyright', '拍摄', '禁止转载'];
const sceneKeywords = ['规格', '价格', '¥', '克', '毫克', '批号'];

if (hasWatermarkKeywords(text)) {
  confidence += 50;  // 明确是水印
} else if (hasSceneKeywords(text)) {
  confidence -= 30;  // 明确是场景文字
}
```

**预期效果**：精确率提升到80%+

### 方案B：深度学习分类器

训练轻量级CNN，区分：
- 水印文字（版权、署名）
- 场景文字（规格、价格）

**预期效果**：准确率95%+

---

## 📝 使用建议

### 当前方案价值

虽然到达像素级方法的瓶颈，但已具备实用价值：

1. **初筛阶段**：快速过滤195张图片中的124张明确无水印
2. **保证不漏**：100%召回率，不会遗漏任何水印
3. **人工复核**：对71个疑似水印进行人工确认
4. **成本优化**：避免对所有200张图片都做昂贵的OCR

### 生产流程建议

```
Step 1: test-watermark-images.js 批量检测
  ↓
  检测结果：76张疑似有水印
  ↓
Step 2: 人工复核或OCR验证（仅71个假阳性）
  ↓
Step 3: 最终确认5张真水印
```

**总成本**：像素检测（200张） + 人工复核（71张） << 全部OCR（200张）

---

## 🔧 技术细节

### 检测器架构

```
输入：200张医院/药店场景图片
  ↓
[像素级特征提取]
  • 文本相似度 (TL)
  • 覆盖一致性 (OC)
  • Alpha透明度 (AL)
  • 位置权重 (PW)
  • 网格特征 (Grid) ← 货架/瓷砖网格
  • ROI局部网格 (ROI Grid)
  • 空间集中度 (Conc)
  • 边缘方向熵 (EdgeEntropy)
  • 白边比例 (Whiteness)
  ↓
[FFT频域分析] (辅助15%)
  • 周期性检测
  • 高频异常
  • 方向集中度
  ↓
[多分支融合]
  • Repeated分支：重复水印
  • Single分支：单个水印 (主要)
  • Baseline分支：基础特征
  ↓
[硬拒绝规则] (10条)
  规则1-9：过滤货架场景文字
  规则10：过滤强网格+低集中度
  ↓
[专门路径] (3条)
  • cornerLogo：角标Logo
  • cornerText：角落文字水印
  • edgeText：边缘文字水印
  ↓
输出：5张真水印 + 71张假阳性
```

---

## 📚 相关文件

- **主检测器**：`test-watermark-images.js`(推荐使用)
- **阈值优化**：`optimize-watermark-threshold.js`
- **结果分析**：`batch-test-watermarks.js`
- **地图水印补充**：`MAP-WATERMARK-NOTES.md`

---

*最后更新：2025-01-17*
*场景澄清：Excel提取图片 → 医院/药店场景 → 货架网格 ≠ Excel网格*
